# Preview inspection notes

The homepage renders with the selected Neo-Brutalist Scientific Atlas design: warm graph-paper background, large Fraunces display title, tactile chips, heavy bordered cards, and the generated hero atlas image. The visible extracted content confirms that the TypeScript operator bench, symbolic expression plate using Nerdamer/custom rule, Recharts trace chart, GL weight chart, PyScript SciPy/SymPy section, kernel field, provenance section, and FCAA ledger all appear in the page content.

The top layout is visually coherent at desktop width. Browser scroll behavior was constrained by the preview overlay during automated inspection, but the extracted full-page markdown verifies that lower sections are present in the DOM.

Build status before preview: `pnpm check` passed and `pnpm build` completed successfully. Vite reported a non-blocking large chunk warning due to the app’s charting/symbolic dependencies and PyScript source payload.
