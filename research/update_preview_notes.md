# Update preview notes

The updated homepage now exposes a top-navigation link for **Derivative starter**. Browser extraction confirms the new section appears before the operator bench and includes explanatory text for `p(x)=0.18x³−0.85x²+1.65x+0.45`, the classical derivative `p'(x)`, and the fractional Riemann–Liouville bridge `D^α p(x)` at `α=0.63`. The chart legend lists `p(x)`, `ordinary derivative p'(x)`, and `fractional derivative D^0.63 p(x)`.

The overall visual style remains consistent with the Neo-Brutalist Scientific Atlas direction: warm paper, visible black rules, graph-paper panels, and research-plate labels.

A later preview click/scroll confirms the derivative starter is positioned as the first instructional section after the hero, with the chart visible and aligned to the atlas visual language. Browser extraction still sees the PyScript section and its source disclosure, but the iframe output itself is not included in the page-level Markdown extraction, so runtime verification needs console-log inspection as the next step.
